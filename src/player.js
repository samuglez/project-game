class Player {
    constructor(atack) {
        const hit = document.getElementsByClassName("player-container")
        atack = "selected"
        hit.addEventListener("Click", (damage)=>{
            if(atack ==="selected" && enemies-container === "selected"){
                enemies.life - hit.atack
            }

        })
        
    }
}
